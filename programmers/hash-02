#include <iostream>
#include <vector>
#include <string>
#include <map>

using namespace std;

bool solution(vector<string> phone_book) {
    bool answer = true;
    map<string, int> numMap;
    for(string p: phone_book){
        for(map<string,int>::iterator m : numMap){
            if(p.length() > m->first.length()){
                for(int i = 1 ;i<=p.length(); i++){
                    string cutStr = p.substr(0, i);
                    if(m->first == cutStr){
                        answer = false;
                        return answer;
                    }
                    
                }
            }
        }
    }
        numMap[p] = 1;

    return answer;
}

int main()
{
    
    
    string pb1[] = {"02", "1919", "049328"};
    string pb2[] = {"02", "1919", "049328", "02345"};
    string pb3[] = {"02", "1919", "049328", "191942"};

    vector<string> phone_book;
    
    for(string p : pb1){
        phone_book.push_back(p);
    }
    
    bool result =  solution(phone_book);
    cout<< result;

    return 0;
}
